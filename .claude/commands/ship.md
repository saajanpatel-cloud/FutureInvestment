# ship

Project health check before build or release. Customize paths for this repo.

1. Run language checks (for example `python3 -m py_compile` on your Python entrypoints, `swift build`, or your CI script).
2. Run asset or codegen sync scripts if your stack has them.
3. Open the IDE project or follow your release checklist doc under `docs/`.
